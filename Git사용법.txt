1. 로그를 꼭 쓰자.

2. 공부하고 싶다면 솔루션 프로젝트 파일에는 존재하는데
   git 변경내용에 없다면 쓸모가 없는 파일이라는것을 인식하고
   왜 쟤는 쓸모가 없지?
   // .vs 는 왜 쓸모가 없지?

3. .git은 지우는 순간 여러분들 폴더와 git서버와의 연결이 끊어지고
   대부분은 그냥 깃에서 시키는대로 하세요

4. 내가 올린 파일이 어떤 역사를 가졌는지 보고 싶다면
   상단메뉴 > git > 분기관리로 가시면 됩니다.